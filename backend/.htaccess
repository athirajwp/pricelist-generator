<IfModule mod_rewrite.c>
    RewriteEngine On
    DirectoryIndex public/index.php index.php index.html

    # Direct all requests to public folder if not already in public/
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>



